services:
  subway-tracker:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - TZ=America/New_York  # Set timezone for accurate day/night switching
    volumes:
      # Optional: mount logs directory if you add logging
      - ./logs:/app/logs